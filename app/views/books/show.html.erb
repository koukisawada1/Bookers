<table>
	<tr class="show-title">
		<td>title:</td>
		<td><%= @book.title %></td>
	</tr>
	<tr class="show-body">
		<td>body:</td>
		<td><%= @book.body %></td>
	</tr>
</table>

<%= link_to "Edit", edit_book_path(@book.id) %>
<%= link_to "Back", books_path %>

